# App-owner configuration. Google Drive appDataFolder still requires an OAuth app registration.
# Public app identifier, not a secret. Set before building the client bundle.
NEXT_PUBLIC_GOOGLE_DRIVE_CLIENT_ID=
